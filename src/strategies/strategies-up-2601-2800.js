/**
 * UP ?�략 ?�장 (ID: 2601-2800)
 * 
 * STRATEGY_IDEAS.txt 기반 체계??구성:
 * - 2601-2800: 복합 캔들 ?�턴 (200�? - 2651-2800 범위
 */

export class StrategiesUp2601 {
    
    // ==================== 복합 캔들 ?�턴 (2601-2800) ====================
    // ?�중 캔들 조합, 추세 ?�인, 지??컨펌
    
    // --- ?�속 ?�봉 ?�턴 (2601-2625) ---
    
    /** ?�봉 ?�속 + 거래???�인 */
    static strategy2601(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.volumeRatio >= 1.2; return { id: 2601, name: '2 Green + Vol 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 68 : 0 }; }
    static strategy2602(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.volumeRatio >= 1.5; return { id: 2602, name: '2 Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2603(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.volumeRatio >= 2.0; return { id: 2603, name: '2 Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2604(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.priceChange > 1; return { id: 2604, name: '2 Green + Price +1%', direction: 'UP', match: Boolean(match), confidence: match ? 70 : 0 }; }
    static strategy2605(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.priceChange > 2; return { id: 2605, name: '2 Green + Price +2%', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    
    /** ?�봉 ?�속 + RSI 컨펌 */
    static strategy2606(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.rsi >= 50; return { id: 2606, name: '2 Green + RSI 50+', direction: 'UP', match: Boolean(match), confidence: match ? 71 : 0 }; }
    static strategy2607(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.rsi >= 55; return { id: 2607, name: '2 Green + RSI 55+', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2608(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.rsi < 40; return { id: 2608, name: '2 Green + RSI <40 Recovery', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2609(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.rsi < 35; return { id: 2609, name: '2 Green + RSI <35 Recovery', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2610(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.rsi < 30; return { id: 2610, name: '2 Green + RSI <30 Recovery', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** ?�봉 ?�속 + MACD 컨펌 */
    static strategy2611(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.macd?.MACD > ind.macd?.signal; return { id: 2611, name: '2 Green + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2612(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.macd?.histogram > 0; return { id: 2612, name: '2 Green + MACD Hist+', direction: 'UP', match: Boolean(match), confidence: match ? 71 : 0 }; }
    static strategy2613(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.macd?.histogram > 10; return { id: 2613, name: '2 Green + MACD Hist 10+', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2614(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.macd?.histogram > 20; return { id: 2614, name: '2 Green + MACD Hist 20+', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2615(ind) { const histTurn = ind.macd?.histogram > (ind.macd?.prevHistogram || -999); const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && histTurn; return { id: 2615, name: '2 Green + MACD Turn', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    
    /** ?�봉 ?�속 + BB 컨펌 */
    static strategy2616(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.bollingerBands?.position < 30; return { id: 2616, name: '2 Green + BB Lower', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2617(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.bollingerBands?.position < 25; return { id: 2617, name: '2 Green + BB <25', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2618(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.bollingerBands?.position < 20; return { id: 2618, name: '2 Green + BB <20', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2619(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.bollingerBands?.position >= 40 && ind.bollingerBands?.position <= 60; return { id: 2619, name: '2 Green + BB Mid', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2620(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.close > ind.bollingerBands?.middle; return { id: 2620, name: '2 Green + Above BB Mid', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    
    /** ?�봉 ?�속 + EMA 컨펌 */
    static strategy2621(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.ema20 > ind.ema50; return { id: 2621, name: '2 Green + EMA Golden', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2622(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.close > ind.ema20; return { id: 2622, name: '2 Green + Above EMA20', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2623(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.close > ind.ema50; return { id: 2623, name: '2 Green + Above EMA50', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2624(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; return { id: 2624, name: '2 Green + EMA Stack', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2625(ind) { const match = ind.close > ind.open && ind.prevClose > ind.prevOpen && ind.close > ind.ema20 && ind.ema20 > ind.ema50; return { id: 2625, name: '2 Green + Full Bull', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    
    // --- ?�승 ?�악???�턴 (2626-2650) ---
    
    /** ?�승 ?�악??기본 (Bullish Engulfing) */
    static strategy2626(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.2; const match = (engulf) && ind.ema10 < ind.ema20; return { id: 2626, name: 'Bullish Engulfing 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2627(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = (engulf) && ind.sma20 > ind.sma50 && ind.volume > (ind.avgVolume || ind.volume) * 1.2000000000000002; return { id: 2627, name: 'Bullish Engulfing 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2628(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 2.0; const match = Boolean(engulf) && ind.volume > (ind.prevVolume || ind.volume) * 1.21; return { id: 2628, name: 'Bullish Engulfing 2x', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    
    /** ?�승 ?�악??+ 거래??*/
    static strategy2629(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.volumeRatio >= 1.2; return { id: 2629, name: 'Engulf + Vol 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2630(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.volumeRatio >= 1.5; return { id: 2630, name: 'Engulf + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2631(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.volumeRatio >= 2.0; return { id: 2631, name: 'Engulf + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** ?�승 ?�악??+ RSI */
    static strategy2632(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi < 30; return { id: 2632, name: 'Engulf + RSI <30', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2633(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi < 35; return { id: 2633, name: 'Engulf + RSI <35', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2634(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi < 40; return { id: 2634, name: 'Engulf + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2635(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi >= 50; return { id: 2635, name: 'Engulf + RSI 50+', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    
    /** ?�승 ?�악??+ MACD */
    static strategy2636(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.macd?.MACD > ind.macd?.signal; return { id: 2636, name: 'Engulf + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2637(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.macd?.histogram > 0; return { id: 2637, name: 'Engulf + MACD Hist+', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2638(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.macd?.histogram > 10; return { id: 2638, name: 'Engulf + MACD Hist10+', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    
    /** ?�승 ?�악??+ BB */
    static strategy2639(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.bollingerBands?.position < 25; return { id: 2639, name: 'Engulf + BB <25', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2640(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.bollingerBands?.position < 20; return { id: 2640, name: 'Engulf + BB <20', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    
    /** ?�승 ?�악??+ EMA */
    static strategy2641(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.ema20 > ind.ema50; return { id: 2641, name: 'Engulf + EMA Golden', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2642(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.close > ind.ema20; return { id: 2642, name: 'Engulf + Above EMA20', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    
    /** ?�승 ?�악??복합 */
    static strategy2643(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2643, name: 'Engulf + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2644(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi < 30 && ind.volumeRatio >= 1.5; return { id: 2644, name: 'Engulf + RSI30 + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2645(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.bollingerBands?.position < 25 && ind.macd?.MACD > ind.macd?.signal; return { id: 2645, name: 'Engulf + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2646(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.ema20 > ind.ema50 && ind.rsi < 40; return { id: 2646, name: 'Engulf + EMA + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2647(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = engulf && ind.volumeRatio >= 2.0 && ind.rsi < 35; return { id: 2647, name: 'Strong Engulf + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    static strategy2648(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 2; const match = engulf && ind.bollingerBands?.position < 20 && ind.macd?.histogram > 5; return { id: 2648, name: 'Strong Engulf + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    static strategy2649(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2649, name: 'Engulf Triple Confirm', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    static strategy2650(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = engulf && ind.rsi < 28 && ind.bollingerBands?.position < 15 && ind.volumeRatio >= 2; return { id: 2650, name: 'Ultimate Engulf Signal', direction: 'UP', match: Boolean(match), confidence: match ? 95 : 0 }; }
    
    // --- 망치???�턴 (2651-2675) ---
    
    /** 망치??기본 (Hammer) - �??�래꼬리 */
    static strategy2651(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer) && ind.sma20 < ind.sma50; return { id: 2651, name: 'Hammer Basic', direction: 'UP', match: Boolean(match), confidence: match ? 70 : 0 }; }
    static strategy2652(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = (hammer) && ind.bollingerBands?.position < 50 && ind.volume > (ind.prevVolume || ind.volume) * 1.09; return { id: 2652, name: 'Hammer Strong', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2653(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 3 && upperWick < body * 0.2; const match = Boolean(hammer) && ind.volume > ind.avgVolume * 1.5; return { id: 2653, name: 'Hammer Very Strong', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    
    /** 망치??+ 거래??*/
    static strategy2654(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.volumeRatio >= 1.2; return { id: 2654, name: 'Hammer + Vol 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2655(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.volumeRatio >= 1.5; return { id: 2655, name: 'Hammer + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2656(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.volumeRatio >= 2.0; return { id: 2656, name: 'Hammer + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    
    /** 망치??+ RSI */
    static strategy2657(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer && ind.rsi < 30) && ind.consecutiveRed <= 2 && ind.volume > ind.avgVolume * 1.1; return { id: 2657, name: 'Hammer + RSI <30', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2658(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.rsi < 35; return { id: 2658, name: 'Hammer + RSI <35', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2659(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.rsi < 40; return { id: 2659, name: 'Hammer + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    
    /** 망치??+ MACD */
    static strategy2660(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer && ind.macd?.MACD > ind.macd?.signal) && ind.ema50 > ind.sma50 && ind.volume > (ind.prevVolume || ind.volume) * 1.05; return { id: 2660, name: 'Hammer + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2661(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer && ind.macd?.histogram > 0) && ind.rsi > 25 && ind.rsi < 75 && ind.volume > ind.avgVolume * 1.5; return { id: 2661, name: 'Hammer + MACD Hist+', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2662(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const histTurn = ind.macd?.histogram > (ind.macd?.prevHistogram || -999); const match = (hammer && histTurn) && ind.rsi > 35 && ind.rsi < 65 && ind.obv > (ind.prevObv || 0); return { id: 2662, name: 'Hammer + MACD Turn', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    
    /** 망치??+ BB */
    static strategy2663(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.bollingerBands?.position < 25; return { id: 2663, name: 'Hammer + BB <25', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2664(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer && ind.bollingerBands?.position < 20) && ind.rsi > 20 && ind.rsi < 80 && ind.volume > (ind.prevVolume || ind.volume) * 1.1300000000000001; return { id: 2664, name: 'Hammer + BB <20', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2665(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer && ind.bollingerBands?.position < 15) && ind.macd?.histogram < 0 && ind.volume > ind.avgVolume * 1.1; return { id: 2665, name: 'Hammer + BB <15', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    
    /** 망치??+ EMA */
    static strategy2666(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (hammer && ind.ema20 > ind.ema50) && ind.macd?.histogram > 10 && ind.obv > (ind.prevObv || 0); return { id: 2666, name: 'Hammer + EMA Golden', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2667(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && Math.abs(ind.close - ind.ema20) / ind.ema20 < 0.01; return { id: 2667, name: 'Hammer Near EMA20', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    
    /** 망치??복합 */
    static strategy2668(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2668, name: 'Hammer + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2669(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.rsi < 30 && ind.volumeRatio >= 1.5; return { id: 2669, name: 'Hammer + RSI30 + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2670(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.bollingerBands?.position < 25 && ind.macd?.MACD > ind.macd?.signal; return { id: 2670, name: 'Hammer + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2671(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.ema20 > ind.ema50 && ind.rsi < 40; return { id: 2671, name: 'Hammer + EMA + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2672(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = hammer && ind.volumeRatio >= 2.0 && ind.rsi < 35; return { id: 2672, name: 'Strong Hammer + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2673(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = hammer && ind.bollingerBands?.position < 20 && ind.macd?.histogram > 5; return { id: 2673, name: 'Strong Hammer + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    static strategy2674(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2674, name: 'Hammer Triple Confirm', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    static strategy2675(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 3 && upperWick < body * 0.2; const match = hammer && ind.rsi < 28 && ind.bollingerBands?.position < 15 && ind.volumeRatio >= 2; return { id: 2675, name: 'Ultimate Hammer Signal', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    
    // --- ?��????�턴 (2676-2700) ---
    
    /** ?��? 기본 (Doji) - 몸통 매우 ?�음 */
    static strategy2676(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = (doji) && ind.bollingerBands?.position > 50; return { id: 2676, name: 'Doji Basic', direction: 'UP', match: Boolean(match), confidence: match ? 60 : 0 }; }
    static strategy2677(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.05; const match = Boolean(doji) && ind.volume > ind.avgVolume * 1.5; return { id: 2677, name: 'Doji Strong', direction: 'UP', match: Boolean(match), confidence: match ? 63 : 0 }; }
    
    /** ?��? + ?�음�??�봉 ?�인 */
    static strategy2678(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.prevClose < ind.prevOpen && ind.close > ind.open; return { id: 2678, name: 'Doji After Red + Green', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2679(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1 && ind.close > ind.open; const match = doji && ind.volumeRatio >= 1.2; return { id: 2679, name: 'Green Doji + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 68 : 0 }; }
    
    /** ?��? + RSI */
    static strategy2680(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = (doji && ind.rsi < 30) && ind.fearGreedIndex > 20 && ind.volume > (ind.prevVolume || ind.volume) * 1.05; return { id: 2680, name: 'Doji + RSI <30', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2681(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.rsi < 35; return { id: 2681, name: 'Doji + RSI <35', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2682(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.rsi < 40; return { id: 2682, name: 'Doji + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 69 : 0 }; }
    
    /** ?��? + MACD */
    static strategy2683(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.macd?.MACD > ind.macd?.signal; return { id: 2683, name: 'Doji + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 71 : 0 }; }
    static strategy2684(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const histTurn = ind.macd?.histogram > (ind.macd?.prevHistogram || -999); const match = doji && histTurn; return { id: 2684, name: 'Doji + MACD Turn', direction: 'UP', match: Boolean(match), confidence: match ? 70 : 0 }; }
    
    /** ?��? + BB */
    static strategy2685(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.bollingerBands?.position < 25; return { id: 2685, name: 'Doji + BB <25', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2686(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.bollingerBands?.position < 20; return { id: 2686, name: 'Doji + BB <20', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2687(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.bollingerBands?.position < 15; return { id: 2687, name: 'Doji + BB <15', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    
    /** ?��? + EMA */
    static strategy2688(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.ema20 > ind.ema50; return { id: 2688, name: 'Doji + EMA Golden', direction: 'UP', match: Boolean(match), confidence: match ? 70 : 0 }; }
    static strategy2689(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && Math.abs(ind.close - ind.ema20) / ind.ema20 < 0.01; return { id: 2689, name: 'Doji Near EMA20', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    
    /** ?��? 복합 */
    static strategy2690(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2690, name: 'Doji + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2691(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.rsi < 30 && ind.volumeRatio >= 1.5; return { id: 2691, name: 'Doji + RSI30 + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2692(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.bollingerBands?.position < 25 && ind.macd?.MACD > ind.macd?.signal; return { id: 2692, name: 'Doji + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2693(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.ema20 > ind.ema50 && ind.rsi < 40; return { id: 2693, name: 'Doji + EMA + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2694(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.05; const match = doji && ind.volumeRatio >= 2.0 && ind.rsi < 35; return { id: 2694, name: 'Strong Doji + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2695(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.05; const match = doji && ind.bollingerBands?.position < 20 && ind.macd?.histogram > 5; return { id: 2695, name: 'Strong Doji + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2696(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2696, name: 'Doji Triple Confirm', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2697(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.05; const match = doji && ind.rsi < 28 && ind.bollingerBands?.position < 15 && ind.volumeRatio >= 2; return { id: 2697, name: 'Ultimate Doji Signal', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    
    /** ?�자리형 ?��? (Dragonfly Doji) - �??�래꼬리 */
    static strategy2698(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const dragonfly = range > 0 && body / range < 0.1 && lowerWick > upperWick * 3; const match = Boolean(dragonfly); return { id: 2698, name: 'Dragonfly Doji', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2699(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const dragonfly = range > 0 && body / range < 0.1 && lowerWick > upperWick * 3; const match = dragonfly && ind.rsi < 35; return { id: 2699, name: 'Dragonfly + RSI <35', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2700(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const dragonfly = range > 0 && body / range < 0.1 && lowerWick > upperWick * 3; const match = dragonfly && ind.rsi < 30 && ind.bollingerBands?.position < 20; return { id: 2700, name: 'Dragonfly + RSI + BB', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    
    // --- 관?�형/모닝?��? ?�턴 (2701-2725) ---
    
    /** 관?�형 (Piercing Line) - ?�봉 ?�음 ?�봉??50% ?�상 관??*/
    static strategy2701(ind) { const prevBody = ind.prevClose - ind.prevOpen; const curBody = ind.close - ind.open; const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = (piercing) && ind.bollingerBands?.position < 30; return { id: 2701, name: 'Piercing Line Basic', direction: 'UP', match: Boolean(match), confidence: match ? 71 : 0 }; }
    static strategy2702(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > ind.prevOpen * 0.6 + ind.prevClose * 0.4; const match = (piercing) && ind.bollingerBands?.position > 70 && ind.obv > (ind.prevObv || 0); return { id: 2702, name: 'Piercing Line 60%', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2703(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > ind.prevOpen * 0.7 + ind.prevClose * 0.3; const match = Boolean(piercing) && ind.volume > (ind.avgVolume || ind.volume) * 1.25; return { id: 2703, name: 'Piercing Line 70%', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    
    /** 관?�형 + 거래??*/
    static strategy2704(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.volumeRatio >= 1.2; return { id: 2704, name: 'Piercing + Vol 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2705(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.volumeRatio >= 1.5; return { id: 2705, name: 'Piercing + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2706(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.volumeRatio >= 2.0; return { id: 2706, name: 'Piercing + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** 관?�형 + RSI */
    static strategy2707(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.rsi < 30; return { id: 2707, name: 'Piercing + RSI <30', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2708(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.rsi < 35; return { id: 2708, name: 'Piercing + RSI <35', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2709(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.rsi < 40; return { id: 2709, name: 'Piercing + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    
    /** 관?�형 + MACD */
    static strategy2710(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.macd?.MACD > ind.macd?.signal; return { id: 2710, name: 'Piercing + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2711(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.macd?.histogram > 0; return { id: 2711, name: 'Piercing + MACD Hist+', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    
    /** 관?�형 + BB */
    static strategy2712(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.bollingerBands?.position < 25; return { id: 2712, name: 'Piercing + BB <25', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2713(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.bollingerBands?.position < 20; return { id: 2713, name: 'Piercing + BB <20', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    
    /** 관?�형 복합 */
    static strategy2714(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2714, name: 'Piercing + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2715(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.rsi < 30 && ind.volumeRatio >= 1.5; return { id: 2715, name: 'Piercing + RSI30 + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2716(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.bollingerBands?.position < 20 && ind.macd?.MACD > ind.macd?.signal; return { id: 2716, name: 'Piercing + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2717(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > ind.prevOpen * 0.7 + ind.prevClose * 0.3; const match = piercing && ind.volumeRatio >= 2.0 && ind.rsi < 35; return { id: 2717, name: 'Strong Piercing + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2718(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2718, name: 'Piercing Triple Confirm', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    /** 모닝?��???(간소??- ?�봉 + ?��?�?+ ?�봉) */
    static strategy2719(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.5; const morning = ind.close > ind.open && smallBody && ind.priceChange > 0.5; const match = (morning) && ind.bollingerBands?.bandwidth > 5; return { id: 2719, name: 'Morning Star Approx', direction: 'UP', match: Boolean(match), confidence: match ? 70 : 0 }; }
    static strategy2720(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.3; const morning = ind.close > ind.open && smallBody && ind.priceChange > 1; const match = Boolean(morning) && ind.volume > (ind.prevVolume || ind.volume) * 1.05; return { id: 2720, name: 'Morning Star Strong', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2721(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.3; const morning = ind.close > ind.open && smallBody && ind.priceChange > 1; const match = morning && ind.rsi < 35; return { id: 2721, name: 'Morning Star + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2722(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.3; const morning = ind.close > ind.open && smallBody && ind.priceChange > 1; const match = morning && ind.volumeRatio >= 1.5; return { id: 2722, name: 'Morning Star + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2723(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.3; const morning = ind.close > ind.open && smallBody && ind.priceChange > 1; const match = morning && ind.macd?.MACD > ind.macd?.signal; return { id: 2723, name: 'Morning Star + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2724(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.3; const morning = ind.close > ind.open && smallBody && ind.priceChange > 1; const match = morning && ind.bollingerBands?.position < 25; return { id: 2724, name: 'Morning Star + BB', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2725(ind) { const smallBody = Math.abs(ind.prevClose - ind.prevOpen) < Math.abs(ind.close - ind.open) * 0.3; const morning = ind.close > ind.open && smallBody && ind.priceChange > 1; const match = morning && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2725, name: 'Morning Star Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    // --- ??��치형/??��??복합 (2726-2750) ---
    
    /** ??��치형 (Inverted Hammer) - �??�꼬�?*/
    static strategy2726(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = (invHammer) && ind.bollingerBands?.bandwidth < 10; return { id: 2726, name: 'Inverted Hammer Basic', direction: 'UP', match: Boolean(match), confidence: match ? 68 : 0 }; }
    static strategy2727(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2.5 && lowerWick < body * 0.3; const match = (invHammer) && ind.volumeRatio > 1.0 && ind.volume > (ind.avgVolume || ind.volume) * 1.2000000000000002; return { id: 2727, name: 'Inverted Hammer Strong', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2728(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 3 && lowerWick < body * 0.2; const match = Boolean(invHammer) && ind.volume > (ind.prevVolume || ind.volume) * 1.21; return { id: 2728, name: 'Inverted Hammer Very Strong', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    
    /** ??��치형 + 거래??*/
    static strategy2729(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.volumeRatio >= 1.2; return { id: 2729, name: 'Inv Hammer + Vol 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 71 : 0 }; }
    static strategy2730(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.volumeRatio >= 1.5; return { id: 2730, name: 'Inv Hammer + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2731(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.volumeRatio >= 2.0; return { id: 2731, name: 'Inv Hammer + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    
    /** ??��치형 + RSI */
    static strategy2732(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.rsi < 30; return { id: 2732, name: 'Inv Hammer + RSI <30', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2733(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.rsi < 35; return { id: 2733, name: 'Inv Hammer + RSI <35', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2734(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.rsi < 40; return { id: 2734, name: 'Inv Hammer + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    
    /** ??��치형 + MACD */
    static strategy2735(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.macd?.MACD > ind.macd?.signal; return { id: 2735, name: 'Inv Hammer + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2736(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.macd?.histogram > 0; return { id: 2736, name: 'Inv Hammer + MACD Hist+', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    
    /** ??��치형 + BB */
    static strategy2737(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.bollingerBands?.position < 25; return { id: 2737, name: 'Inv Hammer + BB <25', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2738(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.bollingerBands?.position < 20; return { id: 2738, name: 'Inv Hammer + BB <20', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    
    /** ??��치형 복합 */
    static strategy2739(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2739, name: 'Inv Hammer + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2740(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.rsi < 30 && ind.volumeRatio >= 1.5; return { id: 2740, name: 'Inv Hammer + RSI30 + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2741(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.bollingerBands?.position < 25 && ind.macd?.MACD > ind.macd?.signal; return { id: 2741, name: 'Inv Hammer + BB + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2742(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2.5 && lowerWick < body * 0.3; const match = invHammer && ind.volumeRatio >= 2.0 && ind.rsi < 35; return { id: 2742, name: 'Strong Inv Hammer + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2743(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const invHammer = ind.close > ind.open && upperWick > body * 2 && lowerWick < body * 0.5; const match = invHammer && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2743, name: 'Inv Hammer Triple Confirm', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    
    /** 강한 ?�봉 (Strong Green Candle) */
    static strategy2744(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.7; return { id: 2744, name: 'Strong Green Body 70%', direction: 'UP', match: Boolean(match), confidence: match ? 70 : 0 }; }
    static strategy2745(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.8; return { id: 2745, name: 'Strong Green Body 80%', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2746(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.8 && ind.volumeRatio >= 1.5; return { id: 2746, name: 'Strong Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2747(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.8 && ind.rsi < 40; return { id: 2747, name: 'Strong Green + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2748(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.8 && ind.macd?.MACD > ind.macd?.signal; return { id: 2748, name: 'Strong Green + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2749(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.8 && ind.bollingerBands?.position < 30; return { id: 2749, name: 'Strong Green + BB Lower', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2750(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.85 && ind.rsi < 35 && ind.volumeRatio >= 2.0; return { id: 2750, name: 'Ultimate Strong Green', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    // --- �?��???�파 ?�턴 (2751-2775) ---
    
    /** �??�승 (Gap Up) */
    static strategy2751(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open; return { id: 2751, name: 'Gap Up + Green', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2752(ind) { const gapPct = ind.prevClose > 0 ? ((ind.open - ind.prevClose) / ind.prevClose) * 100 : 0; const match = gapPct > 0.5 && ind.close > ind.open; return { id: 2752, name: 'Gap Up 0.5% + Green', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2753(ind) { const gapPct = ind.prevClose > 0 ? ((ind.open - ind.prevClose) / ind.prevClose) * 100 : 0; const match = gapPct > 1 && ind.close > ind.open; return { id: 2753, name: 'Gap Up 1% + Green', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    
    /** �??�승 + 거래??*/
    static strategy2754(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2754, name: 'Gap Up + Green + Vol 1.2x', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2755(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2755, name: 'Gap Up + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2756(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2756, name: 'Gap Up + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    
    /** �??�승 + RSI */
    static strategy2757(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.rsi < 40; return { id: 2757, name: 'Gap Up + Green + RSI <40', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2758(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.rsi >= 50 && ind.rsi <= 65; return { id: 2758, name: 'Gap Up + Green + RSI Strong', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    
    /** �??�승 + MACD */
    static strategy2759(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.macd?.MACD > ind.macd?.signal; return { id: 2759, name: 'Gap Up + Green + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2760(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.macd?.histogram > 10; return { id: 2760, name: 'Gap Up + Green + MACD Hist', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    
    /** �??�승 + EMA */
    static strategy2761(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.ema20 > ind.ema50; return { id: 2761, name: 'Gap Up + Green + EMA Golden', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2762(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.close > ind.ema20; return { id: 2762, name: 'Gap Up + Green + Above EMA20', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    
    /** �??�승 복합 */
    static strategy2763(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.rsi < 40 && ind.macd?.MACD > ind.macd?.signal; return { id: 2763, name: 'Gap Up + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2764(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.volumeRatio >= 1.5 && ind.ema20 > ind.ema50; return { id: 2764, name: 'Gap Up + Vol + EMA', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2765(ind) { const gapPct = ind.prevClose > 0 ? ((ind.open - ind.prevClose) / ind.prevClose) * 100 : 0; const match = gapPct > 1 && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.macd?.MACD > ind.macd?.signal; return { id: 2765, name: 'Strong Gap Up Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    /** ?�??�� ?�파 ?�턴 */
    static strategy2766(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open; const match = Boolean(breakout); return { id: 2766, name: 'Prev High Breakout', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2767(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2767, name: 'Breakout + Vol 1.2x', direction: 'UP', match: breakout, confidence: breakout ? 77 : 0 }; }
    static strategy2768(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2768, name: 'Breakout + Vol 1.5x', direction: 'UP', match: breakout, confidence: breakout ? 81 : 0 }; }
    static strategy2769(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2769, name: 'Breakout + Vol 2x', direction: 'UP', match: breakout, confidence: breakout ? 85 : 0 }; }
    static strategy2770(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.macd?.MACD > ind.macd?.signal; return { id: 2770, name: 'Breakout + MACD', direction: 'UP', match: breakout, confidence: breakout ? 79 : 0 }; }
    static strategy2771(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.rsi >= 50 && ind.rsi <= 70; return { id: 2771, name: 'Breakout + RSI Strong', direction: 'UP', match: breakout, confidence: breakout ? 77 : 0 }; }
    static strategy2772(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.ema20 > ind.ema50; return { id: 2772, name: 'Breakout + EMA Golden', direction: 'UP', match: breakout, confidence: breakout ? 78 : 0 }; }
    static strategy2773(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.volumeRatio >= 1.5 && ind.macd?.MACD > ind.macd?.signal; return { id: 2773, name: 'Breakout + Vol + MACD', direction: 'UP', match: breakout, confidence: breakout ? 86 : 0 }; }
    static strategy2774(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.rsi >= 50 && ind.rsi <= 65; return { id: 2774, name: 'Breakout + Vol + RSI', direction: 'UP', match: breakout, confidence: breakout ? 88 : 0 }; }
    static strategy2775(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.macd?.MACD > ind.macd?.signal && ind.ema20 > ind.ema50; return { id: 2775, name: 'Breakout Ultimate', direction: 'UP', match: breakout, confidence: breakout ? 92 : 0 }; }
    
    // --- 최종 복합 캔들 ?�턴 (2776-2800) ---
    
    /** 캔들+지??최종 복합 */
    static strategy2776(ind) { const green = ind.close > ind.open; const match = green && ind.priceChange > 1 && ind.volumeRatio >= 1.5 && ind.rsi < 40; return { id: 2776, name: 'Green +1% Vol RSI', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2777(ind) { const green = ind.close > ind.open; const match = green && ind.priceChange > 1.5 && ind.volumeRatio >= 2.0 && ind.macd?.MACD > ind.macd?.signal; return { id: 2777, name: 'Green +1.5% Vol MACD', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2778(ind) { const green = ind.close > ind.open; const match = green && ind.priceChange > 2 && ind.volumeRatio >= 1.5 && ind.bollingerBands?.position < 30; return { id: 2778, name: 'Green +2% Vol BB', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2779(ind) { const green = ind.close > ind.open; const match = green && ind.priceChange > 1 && ind.ema20 > ind.ema50 && ind.rsi >= 50; return { id: 2779, name: 'Green +1% EMA RSI', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2780(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.7 && ind.volumeRatio >= 1.5 && ind.rsi < 35; return { id: 2780, name: 'Strong Body Vol RSI', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2781(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.7 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 30; return { id: 2781, name: 'Strong Body MACD BB', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2782(ind) { const green = ind.close > ind.open; const greenPrev = ind.prevClose > ind.prevOpen; const match = green && greenPrev && ind.volumeRatio >= 1.5 && ind.rsi < 40 && ind.macd?.MACD > ind.macd?.signal; return { id: 2782, name: '2Green Vol RSI MACD', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2783(ind) { const green = ind.close > ind.open; const match = green && ind.priceChange > 1 && ind.volumeRatio >= 2.0 && ind.rsi < 35 && ind.bollingerBands?.position < 25; return { id: 2783, name: 'Green Price Vol RSI BB', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2784(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = engulf && ind.volumeRatio >= 1.5 && ind.macd?.MACD > ind.macd?.signal && ind.rsi < 40; return { id: 2784, name: 'Engulf Vol MACD RSI', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    static strategy2785(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = hammer && ind.volumeRatio >= 1.5 && ind.rsi < 35 && ind.bollingerBands?.position < 25; return { id: 2785, name: 'Hammer Vol RSI BB', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    static strategy2786(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = doji && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 20; return { id: 2786, name: 'Doji RSI MACD BB', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2787(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = piercing && ind.volumeRatio >= 1.5 && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2787, name: 'Piercing Vol RSI MACD', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    static strategy2788(ind) { const gapUp = ind.open > ind.prevClose; const match = gapUp && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.rsi < 40 && ind.ema20 > ind.ema50; return { id: 2788, name: 'Gap Up Vol RSI EMA', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2789(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open; const match = breakout && ind.volumeRatio >= 2.0 && ind.macd?.histogram > 10 && ind.rsi >= 50; return { id: 2789, name: 'Breakout Vol MACD RSI', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    /** 최종 Ultimate ?�그??*/
    static strategy2790(ind) { const green = ind.close > ind.open; const match = green && ind.priceChange > 2 && ind.volumeRatio >= 2.0 && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 25; return { id: 2790, name: 'Green Ultimate 5-Factor', direction: 'UP', match: Boolean(match), confidence: match ? 93 : 0 }; }
    static strategy2791(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const match = ind.close > ind.open && bodyRatio > 0.8 && ind.volumeRatio >= 2.0 && ind.rsi < 30 && ind.macd?.histogram > 10 && ind.bollingerBands?.position < 20; return { id: 2791, name: 'Strong Candle Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 95 : 0 }; }
    static strategy2792(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = engulf && ind.volumeRatio >= 2.0 && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 20; return { id: 2792, name: 'Engulf Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 96 : 0 }; }
    static strategy2793(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = hammer && ind.volumeRatio >= 2.0 && ind.rsi < 28 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 15; return { id: 2793, name: 'Hammer Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 96 : 0 }; }
    static strategy2794(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > ind.prevOpen * 0.7 + ind.prevClose * 0.3; const match = piercing && ind.volumeRatio >= 2.0 && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 20; return { id: 2794, name: 'Piercing Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 95 : 0 }; }
    static strategy2795(ind) { const gapPct = ind.prevClose > 0 ? ((ind.open - ind.prevClose) / ind.prevClose) * 100 : 0; const match = gapPct > 1 && ind.close > ind.open && ind.volumeRatio >= 2.5 && ind.rsi < 40 && ind.macd?.MACD > ind.macd?.signal && ind.ema20 > ind.ema50; return { id: 2795, name: 'Gap Up Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    static strategy2796(ind) { const breakout = ind.close > ind.prevHigh && ind.close > ind.open; const match = breakout && ind.volumeRatio >= 2.5 && ind.macd?.histogram > 15 && ind.rsi >= 50 && ind.rsi <= 70 && ind.ema20 > ind.ema50; return { id: 2796, name: 'Breakout Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    static strategy2797(ind) { const green = ind.close > ind.open; const greenPrev = ind.prevClose > ind.prevOpen; const match = green && greenPrev && ind.priceChange > 1.5 && ind.volumeRatio >= 2.0 && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 30 && ind.ema20 > ind.ema50; return { id: 2797, name: '2Green 7-Factor Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 97 : 0 }; }
    static strategy2798(ind) { const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = (bodyRatio > 0.8 || engulf) && ind.volumeRatio >= 2.5 && ind.rsi < 28 && ind.macd?.histogram > 10 && ind.bollingerBands?.position < 15 && ind.ema20 > ind.ema50; return { id: 2798, name: 'Candle Pattern 7-Factor', direction: 'UP', match: Boolean(match), confidence: match ? 97 : 0 }; }
    static strategy2799(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const bodyRatio = ind.high !== ind.low ? body / (ind.high - ind.low) : 0; const match = (hammer || bodyRatio > 0.85) && ind.volumeRatio >= 3.0 && ind.rsi < 25 && ind.macd?.MACD > ind.macd?.signal && ind.macd?.histogram > 15 && ind.bollingerBands?.position < 10; return { id: 2799, name: 'Extreme Candle Signal', direction: 'UP', match: Boolean(match), confidence: match ? 98 : 0 }; }
    static strategy2800(ind) { const green = ind.close > ind.open; const bodyRatio = ind.high !== ind.low ? Math.abs(ind.close - ind.open) / (ind.high - ind.low) : 0; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = (engulf || hammer || bodyRatio > 0.9) && ind.priceChange > 2 && ind.volumeRatio >= 3.0 && ind.rsi < 25 && ind.macd?.MACD > ind.macd?.signal && ind.macd?.histogram > 20 && ind.bollingerBands?.position < 10 && ind.ema20 > ind.ema50; return { id: 2800, name: 'Ultimate Candle Buy Signal', direction: 'UP', match: Boolean(match), confidence: match ? 99 : 0 }; }
    
    /** 모든 ?�략 ?�행 (2601-2800) */
    static analyzeAll(indicators) {
        return [
            this.strategy2601(indicators), this.strategy2602(indicators), this.strategy2603(indicators), this.strategy2604(indicators), this.strategy2605(indicators),
            this.strategy2606(indicators), this.strategy2607(indicators), this.strategy2608(indicators), this.strategy2609(indicators), this.strategy2610(indicators),
            this.strategy2611(indicators), this.strategy2612(indicators), this.strategy2613(indicators), this.strategy2614(indicators), this.strategy2615(indicators),
            this.strategy2616(indicators), this.strategy2617(indicators), this.strategy2618(indicators), this.strategy2619(indicators), this.strategy2620(indicators),
            this.strategy2621(indicators), this.strategy2622(indicators), this.strategy2623(indicators), this.strategy2624(indicators), this.strategy2625(indicators),
            this.strategy2626(indicators), this.strategy2627(indicators), this.strategy2628(indicators), this.strategy2629(indicators), this.strategy2630(indicators),
            this.strategy2631(indicators), this.strategy2632(indicators), this.strategy2633(indicators), this.strategy2634(indicators), this.strategy2635(indicators),
            this.strategy2636(indicators), this.strategy2637(indicators), this.strategy2638(indicators), this.strategy2639(indicators), this.strategy2640(indicators),
            this.strategy2641(indicators), this.strategy2642(indicators), this.strategy2643(indicators), this.strategy2644(indicators), this.strategy2645(indicators),
            this.strategy2646(indicators), this.strategy2647(indicators), this.strategy2648(indicators), this.strategy2649(indicators), this.strategy2650(indicators),
            this.strategy2651(indicators), this.strategy2652(indicators), this.strategy2653(indicators), this.strategy2654(indicators), this.strategy2655(indicators),
            this.strategy2656(indicators), this.strategy2657(indicators), this.strategy2658(indicators), this.strategy2659(indicators), this.strategy2660(indicators),
            this.strategy2661(indicators), this.strategy2662(indicators), this.strategy2663(indicators), this.strategy2664(indicators), this.strategy2665(indicators),
            this.strategy2666(indicators), this.strategy2667(indicators), this.strategy2668(indicators), this.strategy2669(indicators), this.strategy2670(indicators),
            this.strategy2671(indicators), this.strategy2672(indicators), this.strategy2673(indicators), this.strategy2674(indicators), this.strategy2675(indicators),
            this.strategy2676(indicators), this.strategy2677(indicators), this.strategy2678(indicators), this.strategy2679(indicators), this.strategy2680(indicators),
            this.strategy2681(indicators), this.strategy2682(indicators), this.strategy2683(indicators), this.strategy2684(indicators), this.strategy2685(indicators),
            this.strategy2686(indicators), this.strategy2687(indicators), this.strategy2688(indicators), this.strategy2689(indicators), this.strategy2690(indicators),
            this.strategy2691(indicators), this.strategy2692(indicators), this.strategy2693(indicators), this.strategy2694(indicators), this.strategy2695(indicators),
            this.strategy2696(indicators), this.strategy2697(indicators), this.strategy2698(indicators), this.strategy2699(indicators), this.strategy2700(indicators),
            this.strategy2701(indicators), this.strategy2702(indicators), this.strategy2703(indicators), this.strategy2704(indicators), this.strategy2705(indicators),
            this.strategy2706(indicators), this.strategy2707(indicators), this.strategy2708(indicators), this.strategy2709(indicators), this.strategy2710(indicators),
            this.strategy2711(indicators), this.strategy2712(indicators), this.strategy2713(indicators), this.strategy2714(indicators), this.strategy2715(indicators),
            this.strategy2716(indicators), this.strategy2717(indicators), this.strategy2718(indicators), this.strategy2719(indicators), this.strategy2720(indicators),
            this.strategy2721(indicators), this.strategy2722(indicators), this.strategy2723(indicators), this.strategy2724(indicators), this.strategy2725(indicators),
            this.strategy2726(indicators), this.strategy2727(indicators), this.strategy2728(indicators), this.strategy2729(indicators), this.strategy2730(indicators),
            this.strategy2731(indicators), this.strategy2732(indicators), this.strategy2733(indicators), this.strategy2734(indicators), this.strategy2735(indicators),
            this.strategy2736(indicators), this.strategy2737(indicators), this.strategy2738(indicators), this.strategy2739(indicators), this.strategy2740(indicators),
            this.strategy2741(indicators), this.strategy2742(indicators), this.strategy2743(indicators), this.strategy2744(indicators), this.strategy2745(indicators),
            this.strategy2746(indicators), this.strategy2747(indicators), this.strategy2748(indicators), this.strategy2749(indicators), this.strategy2750(indicators),
            this.strategy2751(indicators), this.strategy2752(indicators), this.strategy2753(indicators), this.strategy2754(indicators), this.strategy2755(indicators),
            this.strategy2756(indicators), this.strategy2757(indicators), this.strategy2758(indicators), this.strategy2759(indicators), this.strategy2760(indicators),
            this.strategy2761(indicators), this.strategy2762(indicators), this.strategy2763(indicators), this.strategy2764(indicators), this.strategy2765(indicators),
            this.strategy2766(indicators), this.strategy2767(indicators), this.strategy2768(indicators), this.strategy2769(indicators), this.strategy2770(indicators),
            this.strategy2771(indicators), this.strategy2772(indicators), this.strategy2773(indicators), this.strategy2774(indicators), this.strategy2775(indicators),
            this.strategy2776(indicators), this.strategy2777(indicators), this.strategy2778(indicators), this.strategy2779(indicators), this.strategy2780(indicators),
            this.strategy2781(indicators), this.strategy2782(indicators), this.strategy2783(indicators), this.strategy2784(indicators), this.strategy2785(indicators),
            this.strategy2786(indicators), this.strategy2787(indicators), this.strategy2788(indicators), this.strategy2789(indicators), this.strategy2790(indicators),
            this.strategy2791(indicators), this.strategy2792(indicators), this.strategy2793(indicators), this.strategy2794(indicators), this.strategy2795(indicators),
            this.strategy2796(indicators), this.strategy2797(indicators), this.strategy2798(indicators), this.strategy2799(indicators), this.strategy2800(indicators)
        ];
    }
}

