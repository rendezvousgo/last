/**
 * UP ?�략 ?�장 (ID: 2801-3000)
 * 
 * STRATEGY_IDEAS.txt 기반 체계??구성:
 * - 2801-2900: ?�턴 + 지??조합 (100�?
 * - 2901-3000: 지지/?�??�� 기반 (100�?
 */

export class StrategiesUp2801 {
    
    // ==================== ?�턴 + 지??조합 (2801-2900) ====================
    // 캔들 ?�턴�?기술??지?�의 복합 ?�인
    
    // --- RSI 과매??+ 캔들 ?�턴 (2801-2820) ---
    
    /** RSI 과매??+ ?�봉 반전 */
    static strategy2801(ind) { const match = ind.rsi < 30 && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2801, name: 'RSI <30 + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2802(ind) { const match = ind.rsi < 30 && ind.close > ind.open && ind.priceChange > 1; return { id: 2802, name: 'RSI <30 + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2803(ind) { const match = ind.rsi < 25 && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2803, name: 'RSI <25 + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2804(ind) { const match = ind.rsi < 25 && ind.close > ind.open && ind.priceChange > 1; return { id: 2804, name: 'RSI <25 + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2805(ind) { const match = ind.rsi < 20 && ind.close > ind.open; return { id: 2805, name: 'RSI <20 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    
    /** RSI 과매??+ ?�악??*/
    static strategy2806(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = (ind.rsi < 30 && engulf) && ind.volumeRatio < 1.0; return { id: 2806, name: 'RSI <30 + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2807(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = ind.rsi < 30 && engulf && ind.volume > (ind.avgVolume || ind.volume) * 1.2000000000000002; return { id: 2807, name: 'RSI <30 + Strong Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2808(ind) { const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = ind.rsi < 25 && engulf; return { id: 2808, name: 'RSI <25 + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    
    /** RSI 과매??+ 망치??*/
    static strategy2809(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (ind.rsi < 30 && hammer) && ind.volumeRatio > 0.8; return { id: 2809, name: 'RSI <30 + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2810(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = ind.rsi < 30 && hammer && ind.obv > (ind.prevObv || 0); return { id: 2810, name: 'RSI <30 + Strong Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2811(ind) { const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = ind.rsi < 25 && hammer; return { id: 2811, name: 'RSI <25 + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    /** RSI 과매??+ ?��? */
    static strategy2812(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = ind.rsi < 30 && doji; return { id: 2812, name: 'RSI <30 + Doji', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2813(ind) { const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = ind.rsi < 25 && doji; return { id: 2813, name: 'RSI <25 + Doji', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    
    /** RSI 과매??+ 관?�형 */
    static strategy2814(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = ind.rsi < 30 && piercing; return { id: 2814, name: 'RSI <30 + Piercing', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2815(ind) { const piercing = ind.prevClose < ind.prevOpen && ind.close > ind.open && ind.open < ind.prevClose && ind.close > (ind.prevOpen + ind.prevClose) / 2; const match = ind.rsi < 25 && piercing; return { id: 2815, name: 'RSI <25 + Piercing', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    /** RSI 과매??+ 거래??급증 */
    static strategy2816(ind) { const match = ind.rsi < 30 && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2816, name: 'RSI <30 + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2817(ind) { const match = ind.rsi < 30 && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2817, name: 'RSI <30 + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2818(ind) { const match = ind.rsi < 25 && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2818, name: 'RSI <25 + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2819(ind) { const match = ind.rsi < 25 && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2819, name: 'RSI <25 + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    static strategy2820(ind) { const match = ind.rsi < 20 && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2820, name: 'RSI <20 + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    
    // --- MACD 골든?�로??+ 캔들 ?�턴 (2821-2840) ---
    
    /** MACD 골든 + ?�봉 */
    static strategy2821(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2821, name: 'MACD Golden + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2822(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && ind.close > ind.open && ind.priceChange > 1; return { id: 2822, name: 'MACD Golden + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2823(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && ind.close > ind.open && ind.priceChange > 1.5; return { id: 2823, name: 'MACD Golden + Green +1.5%', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    
    /** MACD 골든 + ?�악??*/
    static strategy2824(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = (macdGolden && engulf) && ind.atr > 100; return { id: 2824, name: 'MACD Golden + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2825(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = macdGolden && engulf && ind.volume > ind.avgVolume * 1.1; return { id: 2825, name: 'MACD Golden + Strong Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    
    /** MACD 골든 + 망치??*/
    static strategy2826(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (macdGolden && hammer) && ind.atr < 500; return { id: 2826, name: 'MACD Golden + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2827(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = macdGolden && hammer && ind.volume > (ind.avgVolume || ind.volume) * 1.2000000000000002; return { id: 2827, name: 'MACD Golden + Strong Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    
    /** MACD ?�스?�그??+ ?�봉 */
    static strategy2828(ind) { const histPositive = ind.macd?.histogram > 0; const match = histPositive && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2828, name: 'MACD Hist+ + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2829(ind) { const histStrong = ind.macd?.histogram > 10; const match = (histStrong && ind.close > ind.open && ind.priceChange > 1) && ind.atr > 200; return { id: 2829, name: 'MACD Hist10+ + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2830(ind) { const histStrong = ind.macd?.histogram > 20; const match = histStrong && ind.close > ind.open && ind.priceChange > 1 && ind.obv > (ind.prevObv || 0); return { id: 2830, name: 'MACD Hist20+ + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    
    /** MACD ?�환 + ?�봉 */
    static strategy2831(ind) { const histTurn = ind.macd?.histogram > (ind.macd?.prevHistogram || -999) && ind.macd?.histogram > -10; const match = (histTurn && ind.close > ind.open) && ind.stochastic?.k > ind.stochastic?.d; return { id: 2831, name: 'MACD Turn + Green', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2832(ind) { const histTurn = ind.macd?.histogram > (ind.macd?.prevHistogram || -999) && ind.macd?.histogram > -5; const match = histTurn && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2832, name: 'MACD Turn + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2833(ind) { const histTurn = ind.macd?.histogram > (ind.macd?.prevHistogram || -999) && ind.macd?.prevHistogram < 0 && ind.macd?.histogram > 0; const match = histTurn && ind.close > ind.open && ind.volume > ind.avgVolume * 1.1; return { id: 2833, name: 'MACD Cross Zero + Green', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** MACD + 거래??*/
    static strategy2834(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2834, name: 'MACD Golden + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2835(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2835, name: 'MACD Golden + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2836(ind) { const histStrong = ind.macd?.histogram > 10; const match = histStrong && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2836, name: 'MACD Hist+ + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2837(ind) { const histStrong = ind.macd?.histogram > 10; const match = histStrong && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2837, name: 'MACD Hist+ + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    
    /** MACD 복합 */
    static strategy2838(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 10; const match = macdGolden && histStrong && ind.close > ind.open; return { id: 2838, name: 'MACD Golden + Hist10+ + Green', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2839(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 15; const match = macdGolden && histStrong && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2839, name: 'MACD Strong + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2840(ind) { const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 20; const match = macdGolden && histStrong && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.priceChange > 1; return { id: 2840, name: 'MACD Ultimate + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    
    // --- BB ?�단 + 캔들 ?�턴 (2841-2860) ---
    
    /** BB ?�단 + ?�봉 */
    static strategy2841(ind) { const bbLower = ind.bollingerBands?.position < 25; const match = (bbLower && ind.close > ind.open && ind.priceChange > 0.5) && ind.stochastic?.k < ind.stochastic?.d; return { id: 2841, name: 'BB <25 + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2842(ind) { const bbLower = ind.bollingerBands?.position < 20; const match = (bbLower && ind.close > ind.open && ind.priceChange > 0.5) && ind.stochastic?.k > 30 && ind.obv > (ind.prevObv || 0); return { id: 2842, name: 'BB <20 + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2843(ind) { const bbLower = ind.bollingerBands?.position < 15; const match = bbLower && ind.close > ind.open && ind.priceChange > 0.5 && ind.volume > (ind.avgVolume || ind.volume) * 1.25; return { id: 2843, name: 'BB <15 + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2844(ind) { const bbLower = ind.bollingerBands?.position < 10; const match = bbLower && ind.close > ind.open; return { id: 2844, name: 'BB <10 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    
    /** BB ?�단 + ?�악??*/
    static strategy2845(ind) { const bbLower = ind.bollingerBands?.position < 25; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = (bbLower && engulf) && ind.stochastic?.k < 70; return { id: 2845, name: 'BB <25 + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2846(ind) { const bbLower = ind.bollingerBands?.position < 20; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = (bbLower && engulf) && ind.adx?.adx > 20 && ind.obv > (ind.prevObv || 0); return { id: 2846, name: 'BB <20 + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2847(ind) { const bbLower = ind.bollingerBands?.position < 15; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const match = bbLower && engulf && ind.volume > (ind.avgVolume || ind.volume) * 1.2000000000000002; return { id: 2847, name: 'BB <15 + Strong Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    /** BB ?�단 + 망치??*/
    static strategy2848(ind) { const bbLower = ind.bollingerBands?.position < 25; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (bbLower && hammer) && ind.adx?.adx > 25; return { id: 2848, name: 'BB <25 + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2849(ind) { const bbLower = ind.bollingerBands?.position < 20; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = (bbLower && hammer) && ind.adx?.adx < 40 && ind.volume > ind.avgVolume * 1.1; return { id: 2849, name: 'BB <20 + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2850(ind) { const bbLower = ind.bollingerBands?.position < 15; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const match = bbLower && hammer && ind.obv > (ind.prevObv || 0); return { id: 2850, name: 'BB <15 + Strong Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    /** BB ?�단 + ?��? */
    static strategy2851(ind) { const bbLower = ind.bollingerBands?.position < 25; const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = (bbLower && doji) && ind.adx?.plusDI > ind.adx?.minusDI; return { id: 2851, name: 'BB <25 + Doji', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2852(ind) { const bbLower = ind.bollingerBands?.position < 20; const body = Math.abs(ind.close - ind.open); const range = ind.high - ind.low; const doji = range > 0 && body / range < 0.1; const match = bbLower && doji && ind.volume > (ind.prevVolume || ind.volume) * 1.09; return { id: 2852, name: 'BB <20 + Doji', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    
    /** BB ?�단 + 거래??*/
    static strategy2853(ind) { const bbLower = ind.bollingerBands?.position < 25; const match = (bbLower && ind.close > ind.open && ind.volumeRatio >= 1.5) && ind.priceChange > -1; return { id: 2853, name: 'BB <25 + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2854(ind) { const bbLower = ind.bollingerBands?.position < 20; const match = bbLower && ind.close > ind.open && ind.volumeRatio >= 1.5 && (ind.atr || 0) > (ind.prevAtr || 0); return { id: 2854, name: 'BB <20 + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2855(ind) { const bbLower = ind.bollingerBands?.position < 20; const match = (bbLower && ind.close > ind.open && ind.volumeRatio >= 2.0) && ind.priceChange < 3; return { id: 2855, name: 'BB <20 + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2856(ind) { const bbLower = ind.bollingerBands?.position < 15; const match = bbLower && ind.close > ind.open && ind.volumeRatio >= 2.0 && (ind.atr || 0) > 1.1; return { id: 2856, name: 'BB <15 + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    /** BB ?�퀴즈 + ?�파 */
    static strategy2857(ind) { const bbWidth = ind.bollingerBands?.bandwidth || 0; const squeeze = bbWidth < 5; const match = (squeeze && ind.close > ind.open && ind.priceChange > 1) && ind.priceChange > -2; return { id: 2857, name: 'BB Squeeze + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2858(ind) { const bbWidth = ind.bollingerBands?.bandwidth || 0; const squeeze = bbWidth < 3; const match = squeeze && ind.close > ind.open && ind.priceChange > 1 && ind.obv > (ind.prevObv || 0); return { id: 2858, name: 'BB Tight Squeeze + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2859(ind) { const bbWidth = ind.bollingerBands?.bandwidth || 0; const squeeze = bbWidth < 5; const match = squeeze && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2859, name: 'BB Squeeze + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2860(ind) { const bbWidth = ind.bollingerBands?.bandwidth || 0; const squeeze = bbWidth < 3; const match = squeeze && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.priceChange > 1.5; return { id: 2860, name: 'BB Squeeze Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    // --- EMA 배열 + 캔들 ?�턴 (2861-2880) ---
    
    /** EMA 골든?�로??+ ?�봉 */
    static strategy2861(ind) { const emaGolden = ind.ema20 > ind.ema50; const match = emaGolden && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2861, name: 'EMA Golden + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2862(ind) { const emaGolden = ind.ema20 > ind.ema50; const match = emaGolden && ind.close > ind.open && ind.priceChange > 1; return { id: 2862, name: 'EMA Golden + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2863(ind) { const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = emaStack && ind.close > ind.open; return { id: 2863, name: 'EMA Stack + Green', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2864(ind) { const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = emaStack && ind.close > ind.open && ind.priceChange > 1; return { id: 2864, name: 'EMA Stack + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** EMA 지지 + 반등 */
    static strategy2865(ind) { const nearEma20 = Math.abs(ind.low - ind.ema20) / ind.ema20 < 0.01; const match = nearEma20 && ind.close > ind.open && ind.close > ind.ema20; return { id: 2865, name: 'EMA20 Bounce + Green', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2866(ind) { const nearEma50 = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.01; const match = nearEma50 && ind.close > ind.open && ind.close > ind.ema50; return { id: 2866, name: 'EMA50 Bounce + Green', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2867(ind) { const nearEma20 = Math.abs(ind.low - ind.ema20) / ind.ema20 < 0.01; const match = nearEma20 && ind.close > ind.open && ind.close > ind.ema20 && ind.volumeRatio >= 1.2; return { id: 2867, name: 'EMA20 Bounce + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    
    /** EMA + ?�악??*/
    static strategy2868(ind) { const emaGolden = ind.ema20 > ind.ema50; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = emaGolden && engulf; return { id: 2868, name: 'EMA Golden + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2869(ind) { const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = emaStack && engulf; return { id: 2869, name: 'EMA Stack + Engulf', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    
    /** EMA + 망치??*/
    static strategy2870(ind) { const emaGolden = ind.ema20 > ind.ema50; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = emaGolden && hammer; return { id: 2870, name: 'EMA Golden + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2871(ind) { const nearEma = Math.abs(ind.low - ind.ema20) / ind.ema20 < 0.015; const body = Math.abs(ind.close - ind.open); const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2 && upperWick < body * 0.5; const match = nearEma && hammer; return { id: 2871, name: 'Near EMA20 + Hammer', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    
    /** EMA + 거래??*/
    static strategy2872(ind) { const emaGolden = ind.ema20 > ind.ema50; const match = emaGolden && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2872, name: 'EMA Golden + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2873(ind) { const emaGolden = ind.ema20 > ind.ema50; const match = emaGolden && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2873, name: 'EMA Golden + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2874(ind) { const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = emaStack && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2874, name: 'EMA Stack + Green + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2875(ind) { const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = emaStack && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2875, name: 'EMA Stack + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    
    /** EMA 가�??�파 */
    static strategy2876(ind) { const crossUp = ind.prevClose < ind.ema20 && ind.close > ind.ema20; const match = (crossUp && ind.close > ind.open) && ind.priceChange < 5; return { id: 2876, name: 'EMA20 CrossUp + Green', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2877(ind) { const crossUp = ind.prevClose < ind.ema50 && ind.close > ind.ema50; const match = crossUp && ind.close > ind.open && ind.volume > ind.avgVolume * 1.5; return { id: 2877, name: 'EMA50 CrossUp + Green', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2878(ind) { const crossUp = ind.prevClose < ind.ema20 && ind.close > ind.ema20; const match = (crossUp && ind.close > ind.open && ind.volumeRatio >= 1.5) && ind.consecutiveGreen >= 1; return { id: 2878, name: 'EMA20 CrossUp + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2879(ind) { const crossUp = ind.prevClose < ind.ema50 && ind.close > ind.ema50; const match = crossUp && ind.close > ind.open && ind.volumeRatio >= 1.5 && (ind.atr || 0) > 1.35; return { id: 2879, name: 'EMA50 CrossUp + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2880(ind) { const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const aboveAll = ind.close > ind.ema10; const match = emaStack && aboveAll && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.priceChange > 1; return { id: 2880, name: 'EMA Ultimate Bull', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    // --- 복합 지??+ 캔들 ?�턴 (2881-2900) ---
    
    /** RSI + MACD + ?�봉 */
    static strategy2881(ind) { const match = ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal && ind.close > ind.open; return { id: 2881, name: 'RSI <35 + MACD Golden + Green', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2882(ind) { const match = ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.close > ind.open; return { id: 2882, name: 'RSI <30 + MACD Golden + Green', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2883(ind) { const match = ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.close > ind.open && ind.priceChange > 1; return { id: 2883, name: 'RSI <30 + MACD + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    /** RSI + BB + ?�봉 */
    static strategy2884(ind) { const bbLower = ind.bollingerBands?.position < 25; const match = ind.rsi < 35 && bbLower && ind.close > ind.open; return { id: 2884, name: 'RSI <35 + BB <25 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2885(ind) { const bbLower = ind.bollingerBands?.position < 20; const match = ind.rsi < 30 && bbLower && ind.close > ind.open; return { id: 2885, name: 'RSI <30 + BB <20 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    static strategy2886(ind) { const bbLower = ind.bollingerBands?.position < 15; const match = ind.rsi < 25 && bbLower && ind.close > ind.open; return { id: 2886, name: 'RSI <25 + BB <15 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 93 : 0 }; }
    
    /** MACD + BB + ?�봉 */
    static strategy2887(ind) { const bbLower = ind.bollingerBands?.position < 25; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && bbLower && ind.close > ind.open; return { id: 2887, name: 'MACD Golden + BB <25 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2888(ind) { const bbLower = ind.bollingerBands?.position < 20; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = macdGolden && bbLower && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2888, name: 'MACD + BB <20 + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    
    /** RSI + EMA + ?�봉 */
    static strategy2889(ind) { const emaGolden = ind.ema20 > ind.ema50; const match = ind.rsi < 35 && emaGolden && ind.close > ind.open; return { id: 2889, name: 'RSI <35 + EMA Golden + Green', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2890(ind) { const emaGolden = ind.ema20 > ind.ema50; const match = ind.rsi < 30 && emaGolden && ind.close > ind.open && ind.priceChange > 0.5; return { id: 2890, name: 'RSI <30 + EMA + Green +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    
    /** 3�?지??+ ?�봉 */
    static strategy2891(ind) { const bbLower = ind.bollingerBands?.position < 25; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = ind.rsi < 35 && macdGolden && bbLower && ind.close > ind.open; return { id: 2891, name: 'RSI + MACD + BB + Green', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2892(ind) { const bbLower = ind.bollingerBands?.position < 20; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = ind.rsi < 30 && macdGolden && bbLower && ind.close > ind.open; return { id: 2892, name: 'RSI30 + MACD + BB20 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    static strategy2893(ind) { const emaGolden = ind.ema20 > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = ind.rsi < 35 && macdGolden && emaGolden && ind.close > ind.open; return { id: 2893, name: 'RSI + MACD + EMA + Green', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2894(ind) { const emaGolden = ind.ema20 > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = ind.rsi < 30 && macdGolden && emaGolden && ind.close > ind.open && ind.priceChange > 1; return { id: 2894, name: 'RSI30 + MACD + EMA + Green +1%', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    
    /** 4�?지??+ ?�봉 */
    static strategy2895(ind) { const bbLower = ind.bollingerBands?.position < 25; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const emaGolden = ind.ema20 > ind.ema50; const match = ind.rsi < 35 && macdGolden && bbLower && emaGolden && ind.close > ind.open; return { id: 2895, name: '4-Indicator + Green', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    static strategy2896(ind) { const bbLower = ind.bollingerBands?.position < 20; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const emaGolden = ind.ema20 > ind.ema50; const match = ind.rsi < 30 && macdGolden && bbLower && emaGolden && ind.close > ind.open; return { id: 2896, name: '4-Indicator Strong + Green', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    
    /** 최종 복합 ?�그??*/
    static strategy2897(ind) { const bbLower = ind.bollingerBands?.position < 20; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const emaGolden = ind.ema20 > ind.ema50; const match = ind.rsi < 30 && macdGolden && bbLower && emaGolden && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2897, name: '4-Indicator + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 95 : 0 }; }
    static strategy2898(ind) { const bbLower = ind.bollingerBands?.position < 15; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 10; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = ind.rsi < 28 && macdGolden && histStrong && bbLower && emaStack && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2898, name: 'Ultimate Indicator + Green', direction: 'UP', match: Boolean(match), confidence: match ? 97 : 0 }; }
    static strategy2899(ind) { const bbLower = ind.bollingerBands?.position < 15; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 15; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = ind.rsi < 25 && macdGolden && histStrong && bbLower && emaStack && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.priceChange > 1; return { id: 2899, name: 'Extreme Indicator Signal', direction: 'UP', match: Boolean(match), confidence: match ? 98 : 0 }; }
    static strategy2900(ind) { const bbLower = ind.bollingerBands?.position < 10; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 20; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = ind.rsi < 25 && macdGolden && histStrong && bbLower && emaStack && (engulf || ind.priceChange > 1.5) && ind.volumeRatio >= 2.5; return { id: 2900, name: 'Ultimate Pattern + Indicator', direction: 'UP', match: Boolean(match), confidence: match ? 99 : 0 }; }
    
    // ==================== 지지/?�??�� 기반 (2901-3000) ====================
    // 가�??�벨, ?�봇, 지지/?�???�파
    
    // --- ?�전 �??�??기반 (2901-2925) ---
    
    /** ?�전 ?�??지지 반등 */
    static strategy2901(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = (nearPrevLow && ind.close > ind.open) && ind.consecutiveGreen >= 2; return { id: 2901, name: 'Prev Low Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2902(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.005; const match = nearPrevLow && ind.close > ind.open && ind.obv > (ind.prevObv || 0); return { id: 2902, name: 'Prev Low Exact + Green', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2903(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = nearPrevLow && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2903, name: 'Prev Low + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2904(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = nearPrevLow && ind.close > ind.open && ind.rsi < 35; return { id: 2904, name: 'Prev Low + Green + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2905(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = nearPrevLow && ind.close > ind.open && ind.volumeRatio >= 1.5 && ind.rsi < 35; return { id: 2905, name: 'Prev Low + Green + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    
    /** ?�전 고점 ?�파 */
    static strategy2906(ind) { const breakHigh = ind.prevHigh && ind.close > ind.prevHigh; const match = (breakHigh && ind.close > ind.open) && ind.consecutiveRed <= 2; return { id: 2906, name: 'Prev High Break + Green', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2907(ind) { const breakHigh = ind.prevHigh && ind.close > ind.prevHigh * 1.005; const match = breakHigh && ind.close > ind.open && ind.volume > (ind.avgVolume || ind.volume) * 1.2000000000000002; return { id: 2907, name: 'Prev High Break 0.5% + Green', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2908(ind) { const breakHigh = ind.prevHigh && ind.close > ind.prevHigh; const match = breakHigh && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2908, name: 'Prev High Break + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2909(ind) { const breakHigh = ind.prevHigh && ind.close > ind.prevHigh; const match = breakHigh && ind.close > ind.open && ind.volumeRatio >= 2.0; return { id: 2909, name: 'Prev High Break + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2910(ind) { const breakHigh = ind.prevHigh && ind.close > ind.prevHigh; const match = breakHigh && ind.close > ind.open && ind.macd?.MACD > ind.macd?.signal; return { id: 2910, name: 'Prev High Break + Green + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    
    /** 가�?범위 지지 */
    static strategy2911(ind) { const range = ind.prevHigh - ind.prevLow; const lowerQuarter = ind.prevLow + range * 0.25; const match = ind.low <= lowerQuarter && ind.close > ind.open && ind.close > lowerQuarter; return { id: 2911, name: 'Lower 25% Bounce + Green', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2912(ind) { const range = ind.prevHigh - ind.prevLow; const lowerThird = ind.prevLow + range * 0.33; const match = ind.low <= lowerThird && ind.close > ind.open && ind.close > lowerThird; return { id: 2912, name: 'Lower 33% Bounce + Green', direction: 'UP', match: Boolean(match), confidence: match ? 72 : 0 }; }
    static strategy2913(ind) { const range = ind.prevHigh - ind.prevLow; const lowerQuarter = ind.prevLow + range * 0.25; const match = ind.low <= lowerQuarter && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2913, name: 'Lower 25% Bounce + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    
    /** ?�봇 ?�벨 기반 */
    static strategy2914(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.01; const match = nearS1 && ind.close > ind.open; return { id: 2914, name: 'Pivot S1 Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2915(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.01; const match = nearS1 && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2915, name: 'Pivot S1 + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2916(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r1 = 2 * pivot - ind.prevLow; const breakR1 = ind.close > r1; const match = breakR1 && ind.close > ind.open; return { id: 2916, name: 'Pivot R1 Break + Green', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2917(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r1 = 2 * pivot - ind.prevLow; const breakR1 = ind.close > r1; const match = breakR1 && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2917, name: 'Pivot R1 Break + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** ?�블바�?/?�리?�바?� 근사 */
    static strategy2918(ind) { const doubleLow = ind.prevLow && ind.low2 && Math.abs(ind.prevLow - ind.low2) / ind.prevLow < 0.02; const match = doubleLow && ind.close > ind.open; return { id: 2918, name: 'Double Bottom Approx + Green', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2919(ind) { const doubleLow = ind.prevLow && ind.low2 && Math.abs(ind.prevLow - ind.low2) / ind.prevLow < 0.015; const match = doubleLow && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2919, name: 'Double Bottom + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2920(ind) { const doubleLow = ind.prevLow && ind.low2 && Math.abs(ind.prevLow - ind.low2) / ind.prevLow < 0.015; const match = doubleLow && ind.close > ind.open && ind.rsi < 35; return { id: 2920, name: 'Double Bottom + Green + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    
    /** 복합 지지 ?�벨 */
    static strategy2921(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.01; const match = (nearPrevLow || nearEma) && ind.close > ind.open; return { id: 2921, name: 'Multi Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2922(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearBB = ind.bollingerBands?.position < 15; const match = nearPrevLow && nearBB && ind.close > ind.open; return { id: 2922, name: 'Prev Low + BB + Green', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2923(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearEma = Math.abs(ind.low - ind.ema20) / ind.ema20 < 0.01; const match = nearPrevLow && nearEma && ind.close > ind.open; return { id: 2923, name: 'Prev Low + EMA20 + Green', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2924(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearBB = ind.bollingerBands?.position < 20; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.015; const match = nearPrevLow && nearBB && nearEma && ind.close > ind.open; return { id: 2924, name: 'Triple Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2925(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearBB = ind.bollingerBands?.position < 15; const match = nearPrevLow && nearBB && ind.close > ind.open && ind.volumeRatio >= 1.5 && ind.rsi < 35; return { id: 2925, name: 'Ultimate Support Signal', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    // --- 가�??�벨 기반 (2926-2950) ---
    
    /** ?�수 가격�? 지지 */
    static strategy2926(ind) { const roundNumber = Math.round(ind.close / 1000) * 1000; const nearRound = Math.abs(ind.low - roundNumber) / roundNumber < 0.005; const match = nearRound && ind.close > ind.open; return { id: 2926, name: 'Round Number Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 71 : 0 }; }
    static strategy2927(ind) { const roundNumber = Math.round(ind.close / 1000) * 1000; const nearRound = Math.abs(ind.low - roundNumber) / roundNumber < 0.003; const match = nearRound && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2927, name: 'Round Number + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    
    /** ?�보?�치 ?�벨 근사 */
    static strategy2928(ind) { const range = ind.prevHigh - ind.prevLow; const fib382 = ind.prevHigh - range * 0.382; const nearFib = Math.abs(ind.low - fib382) / fib382 < 0.01; const match = (nearFib && ind.close > ind.open) && ind.fearGreedIndex < 60; return { id: 2928, name: 'Fib 38.2% Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2929(ind) { const range = ind.prevHigh - ind.prevLow; const fib50 = ind.prevHigh - range * 0.5; const nearFib = Math.abs(ind.low - fib50) / fib50 < 0.01; const match = (nearFib && ind.close > ind.open) && ind.fearGreedIndex > 30 && ind.volume > ind.avgVolume * 1.1; return { id: 2929, name: 'Fib 50% Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 76 : 0 }; }
    static strategy2930(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const match = nearFib && ind.close > ind.open && ind.obv > (ind.prevObv || 0); return { id: 2930, name: 'Fib 61.8% Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2931(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const match = nearFib && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2931, name: 'Fib 61.8% + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2932(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const match = nearFib && ind.close > ind.open && ind.rsi < 35; return { id: 2932, name: 'Fib 61.8% + Green + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    
    /** 가�?채널 ?�단 */
    static strategy2933(ind) { const channelLow = ind.low20 || ind.prevLow; const nearChannel = channelLow && Math.abs(ind.low - channelLow) / channelLow < 0.01; const match = nearChannel && ind.close > ind.open; return { id: 2933, name: 'Channel Low + Green', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2934(ind) { const channelLow = ind.low20 || ind.prevLow; const nearChannel = channelLow && Math.abs(ind.low - channelLow) / channelLow < 0.005; const match = nearChannel && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2934, name: 'Channel Low Exact + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    
    /** 가�??�파 ?�벨 */
    static strategy2935(ind) { const high20 = ind.high20 || ind.prevHigh; const breakout = high20 && ind.close > high20; const match = (breakout && ind.close > ind.open) && ind.fearGreedIndex > 20; return { id: 2935, name: '20-Period High Break + Green', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    static strategy2936(ind) { const high20 = ind.high20 || ind.prevHigh; const breakout = high20 && ind.close > high20; const match = (breakout && ind.close > ind.open && ind.volumeRatio >= 1.5) && ind.ema50 > ind.sma50; return { id: 2936, name: '20-Period Break + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2937(ind) { const high20 = ind.high20 || ind.prevHigh; const breakout = high20 && ind.close > high20; const match = (breakout && ind.close > ind.open && ind.volumeRatio >= 2.0) && ind.rsi > 25 && ind.rsi < 75; return { id: 2937, name: '20-Period Break + Green + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    
    /** ATR 기반 지지 */
    static strategy2938(ind) { const atr = ind.atr || ((ind.high - ind.low) * 0.7); const atrSupport = ind.prevClose - atr * 1.5; const nearSupport = ind.low <= atrSupport && ind.close > atrSupport; const match = nearSupport && ind.close > ind.open; return { id: 2938, name: 'ATR Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2939(ind) { const atr = ind.atr || ((ind.high - ind.low) * 0.7); const atrSupport = ind.prevClose - atr * 2; const nearSupport = ind.low <= atrSupport && ind.close > atrSupport; const match = nearSupport && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2939, name: 'ATR 2x Support + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    
    /** 가�?�?지지 */
    static strategy2940(ind) { const gapSupport = ind.prevOpen > ind.prevClose ? ind.prevOpen : ind.prevClose; const nearGap = Math.abs(ind.low - gapSupport) / gapSupport < 0.005; const match = nearGap && ind.close > ind.open; return { id: 2940, name: 'Gap Fill Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 73 : 0 }; }
    static strategy2941(ind) { const gapSupport = ind.prevOpen > ind.prevClose ? ind.prevOpen : ind.prevClose; const nearGap = Math.abs(ind.low - gapSupport) / gapSupport < 0.005; const match = nearGap && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2941, name: 'Gap Fill + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 77 : 0 }; }
    
    /** VWAP 기반 */
    static strategy2942(ind) { const vwap = ind.vwap || ((ind.high + ind.low + ind.close) / 3); const nearVwap = Math.abs(ind.low - vwap) / vwap < 0.01; const match = nearVwap && ind.close > ind.open && ind.close > vwap; return { id: 2942, name: 'VWAP Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 75 : 0 }; }
    static strategy2943(ind) { const vwap = ind.vwap || ((ind.high + ind.low + ind.close) / 3); const nearVwap = Math.abs(ind.low - vwap) / vwap < 0.008; const match = nearVwap && ind.close > ind.open && ind.close > vwap && ind.volumeRatio >= 1.2; return { id: 2943, name: 'VWAP + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    
    /** 복합 가�??�벨 */
    static strategy2944(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.01; const match = (nearFib || nearEma) && ind.close > ind.open; return { id: 2944, name: 'Fib + EMA Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2945(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = nearFib && nearPrevLow && ind.close > ind.open; return { id: 2945, name: 'Fib + Prev Low + Green', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2946(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.01; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.01; const match = nearS1 && nearEma && ind.close > ind.open; return { id: 2946, name: 'Pivot S1 + EMA + Green', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2947(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const bbLower = ind.bollingerBands?.position < 20; const match = nearFib && bbLower && ind.close > ind.open; return { id: 2947, name: 'Fib + BB + Green', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2948(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.01; const bbLower = ind.bollingerBands?.position < 25; const match = nearPrevLow && nearEma && bbLower && ind.close > ind.open; return { id: 2948, name: 'Triple Price Support + Green', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2949(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const bbLower = ind.bollingerBands?.position < 20; const match = nearFib && nearPrevLow && bbLower && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2949, name: 'Ultimate Price Support', direction: 'UP', match: Boolean(match), confidence: match ? 92 : 0 }; }
    static strategy2950(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.008; const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.008; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.008; const bbLower = ind.bollingerBands?.position < 15; const match = (nearFib && nearPrevLow) || (nearEma && bbLower) && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.rsi < 30; return { id: 2950, name: 'Extreme Price Support', direction: 'UP', match: Boolean(match), confidence: match ? 95 : 0 }; }
    
    // --- ?�??�� ?�파 ?�략 (2951-2975) ---
    
    /** ?�순 ?�???�파 */
    static strategy2951(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.volume > (ind.avgVolume || ind.volume) * 1.1500000000000001; return { id: 2951, name: 'Resistance Break Basic', direction: 'UP', match: Boolean(match), confidence: match ? 74 : 0 }; }
    static strategy2952(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.005; const match = (strongBreak && ind.close > ind.open) && ind.rsi > 30 && ind.rsi < 70; return { id: 2952, name: 'Resistance Break +0.5%', direction: 'UP', match: Boolean(match), confidence: match ? 78 : 0 }; }
    static strategy2953(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.01; const match = strongBreak && ind.close > ind.open && ind.volume > ind.avgVolume * 1.1; return { id: 2953, name: 'Resistance Break +1%', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** ?�???�파 + 거래??*/
    static strategy2954(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.volumeRatio >= 1.3; return { id: 2954, name: 'Resistance Break + Vol 1.3x', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2955(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.volumeRatio >= 1.5 && (ind.atr || 0) > 0.75; return { id: 2955, name: 'Resistance Break + Vol 1.5x', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2956(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.volumeRatio >= 2.0 && (ind.atr || 0) > 1.1; return { id: 2956, name: 'Resistance Break + Vol 2x', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2957(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.volumeRatio >= 2.5; return { id: 2957, name: 'Resistance Break + Vol 2.5x', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    
    /** ?�???�파 + MACD */
    static strategy2958(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = breakout && ind.close > ind.open && macdGolden; return { id: 2958, name: 'Resistance + MACD Golden', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2959(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const histStrong = ind.macd?.histogram > 10; const match = breakout && ind.close > ind.open && histStrong; return { id: 2959, name: 'Resistance + MACD Hist 10+', direction: 'UP', match: Boolean(match), confidence: match ? 83 : 0 }; }
    static strategy2960(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = breakout && ind.close > ind.open && macdGolden && ind.volumeRatio >= 1.5; return { id: 2960, name: 'Resistance + MACD + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    
    /** ?�???�파 + RSI */
    static strategy2961(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.rsi >= 50 && ind.rsi <= 65; return { id: 2961, name: 'Resistance + RSI Mid-Strong', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2962(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.rsi >= 55 && ind.rsi <= 70; return { id: 2962, name: 'Resistance + RSI Strong', direction: 'UP', match: Boolean(match), confidence: match ? 81 : 0 }; }
    static strategy2963(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.rsi >= 50 && ind.volumeRatio >= 1.5; return { id: 2963, name: 'Resistance + RSI + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    
    /** ?�???�파 + EMA */
    static strategy2964(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const emaGolden = ind.ema20 > ind.ema50; const match = breakout && ind.close > ind.open && emaGolden; return { id: 2964, name: 'Resistance + EMA Golden', direction: 'UP', match: Boolean(match), confidence: match ? 80 : 0 }; }
    static strategy2965(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = breakout && ind.close > ind.open && emaStack; return { id: 2965, name: 'Resistance + EMA Stack', direction: 'UP', match: Boolean(match), confidence: match ? 84 : 0 }; }
    static strategy2966(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const aboveEma = ind.close > ind.ema20 && ind.close > ind.ema50; const match = breakout && ind.close > ind.open && aboveEma; return { id: 2966, name: 'Resistance + Above EMAs', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    
    /** ?�???�파 복합 */
    static strategy2967(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const emaGolden = ind.ema20 > ind.ema50; const match = breakout && ind.close > ind.open && macdGolden && emaGolden; return { id: 2967, name: 'Resistance + MACD + EMA', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2968(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = breakout && ind.close > ind.open && macdGolden && ind.volumeRatio >= 1.5 && ind.rsi >= 50; return { id: 2968, name: 'Resistance + MACD + Vol + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 88 : 0 }; }
    static strategy2969(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.005; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = strongBreak && ind.close > ind.open && macdGolden && emaStack && ind.volumeRatio >= 1.5; return { id: 2969, name: 'Strong Resistance Break Multi', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    static strategy2970(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.01; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 10; const match = strongBreak && ind.close > ind.open && macdGolden && histStrong && ind.volumeRatio >= 2.0; return { id: 2970, name: 'Ultimate Resistance Break', direction: 'UP', match: Boolean(match), confidence: match ? 93 : 0 }; }
    
    /** ?�봇 ?�???�파 */
    static strategy2971(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r1 = 2 * pivot - ind.prevLow; const r2 = pivot + (ind.prevHigh - ind.prevLow); const breakR1 = ind.close > r1; const match = breakR1 && ind.close > ind.open && ind.volumeRatio >= 1.2; return { id: 2971, name: 'Pivot R1 Break + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 79 : 0 }; }
    static strategy2972(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r2 = pivot + (ind.prevHigh - ind.prevLow); const breakR2 = ind.close > r2; const match = breakR2 && ind.close > ind.open; return { id: 2972, name: 'Pivot R2 Break + Green', direction: 'UP', match: Boolean(match), confidence: match ? 82 : 0 }; }
    static strategy2973(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r2 = pivot + (ind.prevHigh - ind.prevLow); const breakR2 = ind.close > r2; const match = breakR2 && ind.close > ind.open && ind.volumeRatio >= 1.5; return { id: 2973, name: 'Pivot R2 Break + Green + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2974(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r1 = 2 * pivot - ind.prevLow; const breakR1 = ind.close > r1; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const match = breakR1 && ind.close > ind.open && macdGolden && ind.volumeRatio >= 1.5; return { id: 2974, name: 'Pivot R1 + MACD + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2975(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r2 = pivot + (ind.prevHigh - ind.prevLow); const breakR2 = ind.close > r2; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const emaGolden = ind.ema20 > ind.ema50; const match = breakR2 && ind.close > ind.open && macdGolden && emaGolden && ind.volumeRatio >= 2.0; return { id: 2975, name: 'Pivot R2 Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    
    // --- 최종 지지/?�??복합 ?�략 (2976-3000) ---
    
    /** 지지??반등 + 지??복합 */
    static strategy2976(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = nearPrevLow && ind.close > ind.open && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2976, name: 'Support + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2977(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const bbLower = ind.bollingerBands?.position < 20; const match = nearPrevLow && bbLower && ind.close > ind.open && ind.rsi < 30; return { id: 2977, name: 'Support + BB + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2978(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.01; const match = nearPrevLow && nearEma && ind.close > ind.open && ind.macd?.MACD > ind.macd?.signal; return { id: 2978, name: 'Support + EMA + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2979(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const match = nearPrevLow && ind.close > ind.open && ind.rsi < 30 && ind.volumeRatio >= 1.5 && ind.macd?.MACD > ind.macd?.signal; return { id: 2979, name: 'Support Triple Indicator', direction: 'UP', match: Boolean(match), confidence: match ? 90 : 0 }; }
    static strategy2980(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.008; const bbLower = ind.bollingerBands?.position < 15; const match = nearPrevLow && bbLower && ind.close > ind.open && ind.rsi < 28 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 2.0; return { id: 2980, name: 'Support Ultimate Signal', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    
    /** ?�???�파 + 지??복합 */
    static strategy2981(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.rsi >= 50 && ind.rsi <= 65 && ind.macd?.MACD > ind.macd?.signal; return { id: 2981, name: 'Breakout + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 86 : 0 }; }
    static strategy2982(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const emaGolden = ind.ema20 > ind.ema50; const match = breakout && ind.close > ind.open && emaGolden && ind.macd?.MACD > ind.macd?.signal; return { id: 2982, name: 'Breakout + EMA + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2983(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance; const match = breakout && ind.close > ind.open && ind.volumeRatio >= 2.0 && ind.rsi >= 55 && ind.macd?.MACD > ind.macd?.signal; return { id: 2983, name: 'Breakout + Vol + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2984(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.005; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const match = strongBreak && ind.close > ind.open && emaStack && ind.volumeRatio >= 1.5 && ind.macd?.histogram > 10; return { id: 2984, name: 'Strong Breakout Multi', direction: 'UP', match: Boolean(match), confidence: match ? 91 : 0 }; }
    static strategy2985(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.01; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const histStrong = ind.macd?.histogram > 15; const match = strongBreak && ind.close > ind.open && emaStack && histStrong && ind.volumeRatio >= 2.0 && ind.rsi >= 55; return { id: 2985, name: 'Breakout Ultimate Signal', direction: 'UP', match: Boolean(match), confidence: match ? 94 : 0 }; }
    
    /** ?�보?�치 + 지??복합 */
    static strategy2986(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const match = nearFib && ind.close > ind.open && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2986, name: 'Fib 61.8% + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 87 : 0 }; }
    static strategy2987(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const bbLower = ind.bollingerBands?.position < 20; const match = nearFib && bbLower && ind.close > ind.open && ind.rsi < 30; return { id: 2987, name: 'Fib + BB + RSI', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    static strategy2988(ind) { const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.008; const bbLower = ind.bollingerBands?.position < 15; const match = nearFib && bbLower && ind.close > ind.open && ind.rsi < 28 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 1.5; return { id: 2988, name: 'Fib Ultimate Signal', direction: 'UP', match: Boolean(match), confidence: match ? 93 : 0 }; }
    
    /** ?�봇 + 지??복합 */
    static strategy2989(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.01; const match = nearS1 && ind.close > ind.open && ind.rsi < 35 && ind.macd?.MACD > ind.macd?.signal; return { id: 2989, name: 'Pivot S1 + RSI + MACD', direction: 'UP', match: Boolean(match), confidence: match ? 85 : 0 }; }
    static strategy2990(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.008; const bbLower = ind.bollingerBands?.position < 20; const match = nearS1 && bbLower && ind.close > ind.open && ind.rsi < 30 && ind.volumeRatio >= 1.5; return { id: 2990, name: 'Pivot S1 + BB + RSI + Vol', direction: 'UP', match: Boolean(match), confidence: match ? 89 : 0 }; }
    
    /** 최종 Ultimate ?�략 */
    static strategy2991(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.01; const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.01; const support = nearPrevLow || nearFib; const match = support && ind.close > ind.open && ind.rsi < 30 && ind.macd?.MACD > ind.macd?.signal && ind.bollingerBands?.position < 20 && ind.volumeRatio >= 1.5; return { id: 2991, name: 'Multi Support + 4 Indicator', direction: 'UP', match: Boolean(match), confidence: match ? 93 : 0 }; }
    static strategy2992(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.008; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.008; const bbLower = ind.bollingerBands?.position < 15; const match = nearPrevLow && nearEma && bbLower && ind.close > ind.open && ind.rsi < 28 && ind.macd?.MACD > ind.macd?.signal && ind.macd?.histogram > 10 && ind.volumeRatio >= 2.0; return { id: 2992, name: 'Triple Support + 5 Indicator', direction: 'UP', match: Boolean(match), confidence: match ? 95 : 0 }; }
    static strategy2993(ind) { const resistance = ind.prevHigh; const strongBreak = resistance && ind.close > resistance * 1.01; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 15; const match = strongBreak && ind.close > ind.open && emaStack && macdGolden && histStrong && ind.volumeRatio >= 2.5 && ind.rsi >= 55 && ind.rsi <= 70; return { id: 2993, name: 'Ultimate Breakout 6-Factor', direction: 'UP', match: Boolean(match), confidence: match ? 96 : 0 }; }
    static strategy2994(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.005; const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.005; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.005; const confluent = (nearPrevLow && nearFib) || (nearPrevLow && nearEma) || (nearFib && nearEma); const bbLower = ind.bollingerBands?.position < 12; const match = confluent && bbLower && ind.close > ind.open && ind.rsi < 25 && ind.macd?.MACD > ind.macd?.signal && ind.macd?.histogram > 15 && ind.volumeRatio >= 2.5; return { id: 2994, name: 'Confluent Support Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 97 : 0 }; }
    static strategy2995(ind) { const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.005; const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.005; const bbLower = ind.bollingerBands?.position < 10; const emaGolden = ind.ema20 > ind.ema50; const match = nearS1 && nearPrevLow && bbLower && emaGolden && ind.close > ind.open && ind.rsi < 25 && ind.macd?.MACD > ind.macd?.signal && ind.volumeRatio >= 3.0; return { id: 2995, name: 'Pivot + Support Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 97 : 0 }; }
    static strategy2996(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance * 1.015; const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const r2 = pivot + (ind.prevHigh - ind.prevLow); const breakR2 = ind.close > r2; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 20; const match = (breakout || breakR2) && emaStack && macdGolden && histStrong && ind.volumeRatio >= 3.0 && ind.rsi >= 55 && ind.rsi <= 72; return { id: 2996, name: 'Super Breakout Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 98 : 0 }; }
    static strategy2997(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.005; const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.005; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.005; const bbLower = ind.bollingerBands?.position < 8; const tripleSupport = (nearPrevLow && nearFib) || (nearPrevLow && nearEma) || (nearFib && nearEma); const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody; const match = tripleSupport && bbLower && (engulf || ind.priceChange > 1.5) && ind.rsi < 22 && ind.macd?.MACD > ind.macd?.signal && ind.macd?.histogram > 20 && ind.volumeRatio >= 3.0; return { id: 2997, name: 'Triple Support + Engulf Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 98 : 0 }; }
    static strategy2998(ind) { const resistance = ind.prevHigh; const breakout = resistance && ind.close > resistance * 1.02; const high20 = ind.high20 || ind.prevHigh; const break20 = high20 && ind.close > high20 * 1.01; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const aboveAll = ind.close > ind.ema10 && ind.close > ind.ema20 && ind.close > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 25; const match = (breakout || break20) && emaStack && aboveAll && macdGolden && histStrong && ind.volumeRatio >= 3.5 && ind.rsi >= 58 && ind.rsi <= 75; return { id: 2998, name: 'Extreme Breakout Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 99 : 0 }; }
    static strategy2999(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.003; const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.003; const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.003; const nearEma = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.003; const bbLower = ind.bollingerBands?.position < 5; const multiSupport = [nearPrevLow, nearFib, nearS1, nearEma].filter(Boolean).length >= 2; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 1.5; const emaGolden = ind.ema20 > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 25; const match = multiSupport && bbLower && (engulf || ind.priceChange > 2) && emaGolden && macdGolden && histStrong && ind.rsi < 20 && ind.volumeRatio >= 4.0; return { id: 2999, name: 'Extreme Support Ultimate', direction: 'UP', match: Boolean(match), confidence: match ? 99 : 0 }; }
    static strategy3000(ind) { const nearPrevLow = ind.prevLow && Math.abs(ind.low - ind.prevLow) / ind.prevLow < 0.003; const range = ind.prevHigh - ind.prevLow; const fib618 = ind.prevHigh - range * 0.618; const nearFib = Math.abs(ind.low - fib618) / fib618 < 0.003; const pivot = (ind.prevHigh + ind.prevLow + ind.prevClose) / 3; const s1 = 2 * pivot - ind.prevHigh; const nearS1 = Math.abs(ind.low - s1) / s1 < 0.003; const nearEma50 = Math.abs(ind.low - ind.ema50) / ind.ema50 < 0.003; const nearEma20 = Math.abs(ind.low - ind.ema20) / ind.ema20 < 0.003; const bbLower = ind.bollingerBands?.position < 3; const supportCount = [nearPrevLow, nearFib, nearS1, nearEma50, nearEma20].filter(Boolean).length; const body = Math.abs(ind.close - ind.open); const prevBody = Math.abs(ind.prevClose - ind.prevOpen); const engulf = ind.close > ind.open && ind.prevClose < ind.prevOpen && body > prevBody * 2; const lowerWick = Math.min(ind.close, ind.open) - ind.low; const upperWick = ind.high - Math.max(ind.close, ind.open); const hammer = ind.close > ind.open && lowerWick > body * 2.5 && upperWick < body * 0.3; const strongPattern = engulf || hammer || ind.priceChange > 2.5; const emaStack = ind.ema10 > ind.ema20 && ind.ema20 > ind.ema50; const macdGolden = ind.macd?.MACD > ind.macd?.signal; const histStrong = ind.macd?.histogram > 30; const match = supportCount >= 3 && bbLower && strongPattern && emaStack && macdGolden && histStrong && ind.rsi < 18 && ind.volumeRatio >= 5.0; return { id: 3000, name: 'ULTIMATE BUY SIGNAL', direction: 'UP', match: Boolean(match), confidence: match ? 100 : 0 }; }
    
    /** 모든 ?�략 ?�행 (2801-3000) */
    static analyzeAll(indicators) {
        return [
            this.strategy2801(indicators), this.strategy2802(indicators), this.strategy2803(indicators), this.strategy2804(indicators), this.strategy2805(indicators),
            this.strategy2806(indicators), this.strategy2807(indicators), this.strategy2808(indicators), this.strategy2809(indicators), this.strategy2810(indicators),
            this.strategy2811(indicators), this.strategy2812(indicators), this.strategy2813(indicators), this.strategy2814(indicators), this.strategy2815(indicators),
            this.strategy2816(indicators), this.strategy2817(indicators), this.strategy2818(indicators), this.strategy2819(indicators), this.strategy2820(indicators),
            this.strategy2821(indicators), this.strategy2822(indicators), this.strategy2823(indicators), this.strategy2824(indicators), this.strategy2825(indicators),
            this.strategy2826(indicators), this.strategy2827(indicators), this.strategy2828(indicators), this.strategy2829(indicators), this.strategy2830(indicators),
            this.strategy2831(indicators), this.strategy2832(indicators), this.strategy2833(indicators), this.strategy2834(indicators), this.strategy2835(indicators),
            this.strategy2836(indicators), this.strategy2837(indicators), this.strategy2838(indicators), this.strategy2839(indicators), this.strategy2840(indicators),
            this.strategy2841(indicators), this.strategy2842(indicators), this.strategy2843(indicators), this.strategy2844(indicators), this.strategy2845(indicators),
            this.strategy2846(indicators), this.strategy2847(indicators), this.strategy2848(indicators), this.strategy2849(indicators), this.strategy2850(indicators),
            this.strategy2851(indicators), this.strategy2852(indicators), this.strategy2853(indicators), this.strategy2854(indicators), this.strategy2855(indicators),
            this.strategy2856(indicators), this.strategy2857(indicators), this.strategy2858(indicators), this.strategy2859(indicators), this.strategy2860(indicators),
            this.strategy2861(indicators), this.strategy2862(indicators), this.strategy2863(indicators), this.strategy2864(indicators), this.strategy2865(indicators),
            this.strategy2866(indicators), this.strategy2867(indicators), this.strategy2868(indicators), this.strategy2869(indicators), this.strategy2870(indicators),
            this.strategy2871(indicators), this.strategy2872(indicators), this.strategy2873(indicators), this.strategy2874(indicators), this.strategy2875(indicators),
            this.strategy2876(indicators), this.strategy2877(indicators), this.strategy2878(indicators), this.strategy2879(indicators), this.strategy2880(indicators),
            this.strategy2881(indicators), this.strategy2882(indicators), this.strategy2883(indicators), this.strategy2884(indicators), this.strategy2885(indicators),
            this.strategy2886(indicators), this.strategy2887(indicators), this.strategy2888(indicators), this.strategy2889(indicators), this.strategy2890(indicators),
            this.strategy2891(indicators), this.strategy2892(indicators), this.strategy2893(indicators), this.strategy2894(indicators), this.strategy2895(indicators),
            this.strategy2896(indicators), this.strategy2897(indicators), this.strategy2898(indicators), this.strategy2899(indicators), this.strategy2900(indicators),
            this.strategy2901(indicators), this.strategy2902(indicators), this.strategy2903(indicators), this.strategy2904(indicators), this.strategy2905(indicators),
            this.strategy2906(indicators), this.strategy2907(indicators), this.strategy2908(indicators), this.strategy2909(indicators), this.strategy2910(indicators),
            this.strategy2911(indicators), this.strategy2912(indicators), this.strategy2913(indicators), this.strategy2914(indicators), this.strategy2915(indicators),
            this.strategy2916(indicators), this.strategy2917(indicators), this.strategy2918(indicators), this.strategy2919(indicators), this.strategy2920(indicators),
            this.strategy2921(indicators), this.strategy2922(indicators), this.strategy2923(indicators), this.strategy2924(indicators), this.strategy2925(indicators),
            this.strategy2926(indicators), this.strategy2927(indicators), this.strategy2928(indicators), this.strategy2929(indicators), this.strategy2930(indicators),
            this.strategy2931(indicators), this.strategy2932(indicators), this.strategy2933(indicators), this.strategy2934(indicators), this.strategy2935(indicators),
            this.strategy2936(indicators), this.strategy2937(indicators), this.strategy2938(indicators), this.strategy2939(indicators), this.strategy2940(indicators),
            this.strategy2941(indicators), this.strategy2942(indicators), this.strategy2943(indicators), this.strategy2944(indicators), this.strategy2945(indicators),
            this.strategy2946(indicators), this.strategy2947(indicators), this.strategy2948(indicators), this.strategy2949(indicators), this.strategy2950(indicators),
            this.strategy2951(indicators), this.strategy2952(indicators), this.strategy2953(indicators), this.strategy2954(indicators), this.strategy2955(indicators),
            this.strategy2956(indicators), this.strategy2957(indicators), this.strategy2958(indicators), this.strategy2959(indicators), this.strategy2960(indicators),
            this.strategy2961(indicators), this.strategy2962(indicators), this.strategy2963(indicators), this.strategy2964(indicators), this.strategy2965(indicators),
            this.strategy2966(indicators), this.strategy2967(indicators), this.strategy2968(indicators), this.strategy2969(indicators), this.strategy2970(indicators),
            this.strategy2971(indicators), this.strategy2972(indicators), this.strategy2973(indicators), this.strategy2974(indicators), this.strategy2975(indicators),
            this.strategy2976(indicators), this.strategy2977(indicators), this.strategy2978(indicators), this.strategy2979(indicators), this.strategy2980(indicators),
            this.strategy2981(indicators), this.strategy2982(indicators), this.strategy2983(indicators), this.strategy2984(indicators), this.strategy2985(indicators),
            this.strategy2986(indicators), this.strategy2987(indicators), this.strategy2988(indicators), this.strategy2989(indicators), this.strategy2990(indicators),
            this.strategy2991(indicators), this.strategy2992(indicators), this.strategy2993(indicators), this.strategy2994(indicators), this.strategy2995(indicators),
            this.strategy2996(indicators), this.strategy2997(indicators), this.strategy2998(indicators), this.strategy2999(indicators), this.strategy3000(indicators)
        ];
    }
}

